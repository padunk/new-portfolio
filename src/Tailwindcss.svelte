<style global>
  @tailwind base;
  body {
    @apply p-0 m-0 bg-black box-border max-h-screen max-w-full subpixel-antialiased font-body overflow-x-hidden;
  }

  blockquote,
  dl,
  dd,
  h1,
  h2,
  h3,
  h4,
  h5,
  h6,
  figure,
  p,
  pre {
    @apply m-0;
  }

  a {
    @apply no-underline;
  }
  @tailwind components;

  @tailwind utilities;
</style>
